apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: logging
spec:
  bindings:
  steps:
  - name: install doris cluster
    try:
    - apply:
        file: doris.yaml
    - assert:
        file: doris-assert.yaml
